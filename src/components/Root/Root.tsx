import Router from '../../Router';
import React, { Component, ReactNode } from 'react';

class Root extends Component {
  render(): ReactNode {
    return (
      <>
        <Router />
      </>
    );
  }
}

export default Root;
